/cairn
/cairn.exe
/bin/
/dist/
*.test
*.prof
coverage.out
.cairn/
.gitnexus
.claude/settings.local.json
# cairn setup installs plugin skills into .claude/skills/ as a transient
# install artifact. Canonical source lives in skills/; .claude/skills is
# regenerated by `cairn setup`. The gitnexus skill is pre-existing and
# already tracked — leave it alone by naming the three cairn ones
# explicitly instead of blanket-ignoring the directory.
.claude/skills/using-cairn/
.claude/skills/subagent-driven-development-with-verdicts/
.claude/skills/verdict-backed-verification/
docs/SIGNPATH_APPLICATION.md
bash.exe.stackdump
